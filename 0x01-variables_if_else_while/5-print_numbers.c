#include <stdio.h>
/**
 * main - Entry poin
 * Description - prints number base 10	
 * Return: always 0 (success)
 */
int main(void)
{
	int n = 0;

	if (n = 0; n <= 9; a++);
	{ 
		putchar(a + '0');
	} 
	putchar('\n');
	return (0);

} 


#include "main.h"

/**
 * main - check the code
 *print_alphabet - this is the function of the task
 * Return: Always 0.
 */
void print_alphabet(void)

{
	char c;
	for (c = 'a'; c <= 'z'; c++)
        {  
           putchar(c);
        } 
         putchar('\n')

} 
